{
  "name": "@vunk-formaker/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "run-p type-check \"build-only {@}\" --",
    "preview": "vite preview",
    "build-only": "vite build",
    "type-check": "vue-tsc --build"
  },
  "dependencies": {
    "@vunk/markdown": "file:..\\entry"
  },
  "devDependencies": {
    "vite-plugin-vue-devtools": "^8.1.2"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
